#!/bin/bash

echo '#################################################'
echo '#################################################'
echo '#################################################'
find /usr -name "*ngspice*"
echo '#################################################'
apt-cache search ngspice
echo '#################################################'
ls -l /usr/lib/ngspice
echo '#################################################'
